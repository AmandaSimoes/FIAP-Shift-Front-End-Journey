/**
 * 3 formas de declarar variáveis:
 * 
 * 1. let
 * 2. const
 * 3. var (não usa mais)
 */

const a = 10; // não pode ser reatribuído um valor. 
let b; // não precisa ser inicializado e pode ser reatribuído. 
var c; // tem os mesmo critérios do let, porém, com resalvas; problemas de escopo. 